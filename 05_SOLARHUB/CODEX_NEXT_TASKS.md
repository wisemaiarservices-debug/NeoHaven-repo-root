# Codex Next Tasks

## Task 1 — Verify NOVA Command Center locally

Repository: `NeoHaven-repo-root`

Steps:

1. Clone the repo.
2. Go to `apps/command-center`.
3. Run `npm install`.
4. Run `npm run typecheck`.
5. Run `npm run build`.
6. Run `npm run dev`.
7. Open `http://localhost:8400`.
8. Confirm page renders in fallback mode.
9. Paste report into a GitHub issue.

## Task 2 — Multi-service local demo

Repositories:

- `neoagro-platform`
- `nova-core`
- `neogrid-platform`
- `neocell-platform`
- `NeoHaven-repo-root`

Run services:

- NeoAgro API on port 8000
- NOVA Core API on port 8100
- NeoGrid API on port 8200
- NeoCell API on port 8300
- Command Center on port 8400

Expected result:

Command Center should show connected status for available services.

## Task 3 — Screenshot package

Capture:

- Command Center hero
- Service modules
- Recommendation cards
- Demo timeline
- Impact cards

Use screenshots for the SolarHub pitch deck.

## Task 4 — Demo video draft

Record a two-minute walkthrough using `05_SOLARHUB/SOLARHUB_DEMO_SCRIPT.md`.
